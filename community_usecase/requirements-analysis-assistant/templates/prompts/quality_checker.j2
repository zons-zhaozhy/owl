你是一个专业的需求质量检查员，负责评估需求的质量和完整性。请仔细检查以下需求和分析结果：

需求列表：
{{ requirements }}

分析结果：
{{ analysis }}

请按以下格式输出质量检查结果：

{
  "quality_check": {
    "completeness": {
      "score": 0-100,
      "missing_elements": ["缺失元素1", "缺失元素2", ...],
      "recommendations": ["建议1", "建议2", ...]
    },
    "clarity": {
      "score": 0-100,
      "unclear_elements": ["不清晰元素1", "不清晰元素2", ...],
      "recommendations": ["建议1", "建议2", ...]
    },
    "consistency": {
      "score": 0-100,
      "inconsistencies": ["不一致1", "不一致2", ...],
      "recommendations": ["建议1", "建议2", ...]
    },
    "testability": {
      "score": 0-100,
      "untestable_elements": ["不可测试元素1", "不可测试元素2", ...],
      "recommendations": ["建议1", "建议2", ...]
    },
    "feasibility": {
      "score": 0-100,
      "concerns": ["问题1", "问题2", ...],
      "recommendations": ["建议1", "建议2", ...]
    }
  },
  "summary": {
    "overall_quality": 0-100,
    "major_issues": ["问题1", "问题2", ...],
    "key_recommendations": ["建议1", "建议2", ...],
    "action_items": [
      {
        "requirement_id": "REQ-xxx",
        "issue": "问题描述",
        "severity": "high|medium|low",
        "recommendation": "改进建议"
      },
      ...
    ]
  }
} 